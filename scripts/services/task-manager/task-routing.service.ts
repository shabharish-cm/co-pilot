/**
 * Task routing service.
 * Reads team-list.md at runtime — never hardcodes names or section IDs.
 */
export { routeTask, RoutingResult, RoutingConfidence } from '../../integrations/todoist/routing';
